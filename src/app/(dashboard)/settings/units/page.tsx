import UnitsManagementClient from '@/components/settings/UnitsManagementClient';

export const metadata = {
    title: 'إدارة الوحدات | PillPay',
};

export default function UnitsPage() {
    return <UnitsManagementClient />;
}
